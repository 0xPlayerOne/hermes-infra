# Hourly APFS Snapshot

**Schedule:** Every hour on the hour

**Prompt:**

Take an APFS local snapshot for backup safety: run `tmutil snapshot` (no agent/LLM needed — this is a shell watchdog). Report the snapshot name/location, or skip silently if it fails.
